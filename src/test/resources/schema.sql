CREATE TABLE Books (
    isbn VARCHAR(10),
    author VARCHAR(100),
    title VARCHAR(100),
    genre VARCHAR(50),
    pages INT);

CREATE TABLE Users (
    fullname VARCHAR(100),
    username VARCHAR(30),
    password VARCHAR(50));